# === Local (Windows / Mac) ===
DATABASE_URL="file:./prisma/r14.db"

# === Railway (pegar en el panel de Variables del servicio; volumen montado en /data) ===
# DATABASE_URL=file:/data/r14.db
# UPLOADS_DIR=/data/uploads
# (sin comillas en Railway suele ser más simple)

PORT=3002

# Google Maps / Directions (opcional)
# GOOGLE_MAPS_API_KEY=
# GOOGLE_DIRECTIONS_API_KEY=
